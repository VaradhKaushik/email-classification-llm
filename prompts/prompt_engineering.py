import dspy
from dspy import BootstrapFewShot
import os
import json

# Logging Config

# DSPy Config

# Load environment variables

# DSPy ClassifyEmailSingature(dspy.Signature):

# Load the labelled data

# Preprocess the data

# Split into training and eval set

# Evaluation Metric (for classification it will be the accuracy)

# Optimization Function (trainset, evalset, metric, Optimizer=BootstrapFewShot, )

# Loop through

# Store results in a csv to be manually checked later (needs to be checked to check for overfitting with the few-shot examples)

# Stored in the "prompts/prompt_log.txt" file with the changelog added and version updated.






